[project]
name = "ai-platform-engineering-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Prateek1771", email = "hitliprateek6@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ai-platform-engineering-lab = "engineering_lab:main"

[tool.uv.build-backend]
module-name = "engineering_lab"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "ruff>=0.16.3",
]
